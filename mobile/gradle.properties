org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true

kotlin.code.style=official
kotlin.mpp.androidSourceSetLayoutVersion=2

android.useAndroidX=true
android.nonTransitiveRClass=true

# Compose Multiplatform iOS framework
kotlin.native.cacheKind=none

# ── Backend base URLs ────────────────────────────────────────────────────────
# Override the dev defaults (loopback for the simulator/emulator) by uncommenting
# below or by passing -PIOS_BASE_URL=… on the Gradle CLI.
# When testing on a physical iPhone, paste your ngrok HTTPS URL here.
 IOS_BASE_URL=https://stormbound-unproofread-gianna.ngrok-free.dev/api/v1
# ANDROID_BASE_URL=https://abcd-1234-5678.ngrok-free.app/api/v1

